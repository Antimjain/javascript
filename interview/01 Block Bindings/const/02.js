const person = {
    name: "nick",
}
 
person.name = "antim";
console.log(person.name); //"antim"

const person = {
    name : "vaibhav",
}
console.log(person.name); //error Identifier 'person' has already been declared