variable typing 
    let i = 10;
    i = "antim"

which kw is used to print the text on the screen
    document.write("hiiii)

how do u remove focus
    blur()

types of error
    syntax
    logical
    runtime

push() => add element in array append
pop()  => remove the last element
unshift()  => add element in array prepend

decode = url
encode = u%rl
escape => encode => decode

webgarden many processors in single server
webform   uses more servers
both are web hosting system

